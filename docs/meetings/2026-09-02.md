# 会议纪要 — 2026-09-02

> 会议类型：钉钉管道收尾 + 架构答疑
> 记录：Xiaoxin | 平台：CLI

## 一、钉钉管道待办执行（#2 #3）

### 已完成
1. **Mela SOUL.md 重写为 DEV 人设**（`~/.hermes/profiles/mela/SOUL.md`）
   - 移除克隆残留的 Xiaoxin 协调者人设
   - 角色表对齐：Carlo=PM / Xiaoxin=SE / Workbuddy=QA / Mela=DEV
   - 实现工作流、审批、响应风格全部 DEV 化
2. **WSL 自启脚本加 gateway-mela**（`~/start-webui.sh`）
   - 新增 Mela 网关拉起段（纯钉钉、无代理）
   - WSL 重启后 Mela 自动上线
3. **手册更新**（`references/dingtalk-pipeline-setup.md`）
   - 待办勾掉 2 项（Mela 人设 / 自启脚本）
   - 新增坑位 #7：gateway 进程树内不能 kill gateway（安全拦截，SIGTERM 传播）；重启用
     `tmux send-keys -t <session> C-c` 或重建会话；C-c 可能连带退出外层 shell
   - 新增坑位 #8：`Card edit failed: StreamingUpdateRequest` 为已知无害警告，不影响收发

### 验证
- Mela 网关重启成功：`✓ dingtalk connected`，PID 12492
- 自启脚本 `bash -n` 通过
- 提交 `1e8dc0d` → team-workflow

## 二、架构答疑（Carlo 问，Xiaoxin 答）

| 问题 | 结论 |
|------|------|
| CLI 不打开，钉钉 bot 还能跑吗？ | 能。bot 由后台 tmux 会话（gateway / gateway-mela）提供，独立于任何终端 |
| tmux 会话运行在哪？ | **WSL2 内部**（Linux 守护进程，PPID=1）。Windows 任务管理器显示为 vmmemWSL；Windows 重启即丢，由 start-webui.sh 自启重建 |
| XiaoxinBot 正常工作前提？ | Windows 开机 + WSL 启动（自启脚本全自动）。常关机场景可考虑迁云服务器 |
| CLI 不打开期间的钉钉会话/记忆隔离吗？ | **不隔离，完全共享**。同一 profile（discord-xiaoxin）的 memories/、state.db、sessions/ 全共用；仅"当前活跃上下文窗口"不同，可用 session_search 检索 |

## 三、剩余待办

- [ ] Workbuddy-Bot 接入（需 Carlo 钉钉开放平台建应用 → 拿 Client ID/Secret）
- [ ] 实测 bot 间 @ 接力是否生效（否则统一用唤醒词）
- [ ] 协议细节微调（可选）
- [ ] （若常关机）评估云服务器迁移

## 四、新约定

- **Carlo 说"会话结束"→ 必须总结本次会话并 git commit+push**（已存入长期记忆，每次生效）
